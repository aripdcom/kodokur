<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Promise: the only permission is the camera. No INTERNET; everything such as
         book lookup or opening a link is handed to the browser or the relevant app
         via an intent. Gallery images come through the system picker (no permission
         needed); CSV sharing uses FileProvider to grant read access to that one file. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Devices without a camera can install too: reading from the gallery works. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- Signature-level permission merged in from androidx.core's own manifest (see Reyon).
         Kodokur does not call registerReceiver(…, RECEIVER_NOT_EXPORTED); if anyone
         adds such a call, remove these two entries or it crashes on API 26–32. -->
    <permission
        android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />
    <uses-permission
        android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />

    <!-- History may contain Wi-Fi passwords: it is kept out of cloud backup and stays on the device. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:appCategory="productivity"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kodokur"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.share"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>
    </application>

</manifest>
